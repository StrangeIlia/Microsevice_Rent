spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/microservice_rent?useUnicode=true&serverTimezone=UTC
    driverClassName: com.mysql.cj.jdbc.Driver
    username: ilya
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
server:
  port: 50001